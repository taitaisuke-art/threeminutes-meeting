.header
  %h1 青色会議
  %p (会議時間短縮の為のサイト)
.descripition
  %h1 サイト理念
  %p  1.物事を端的にまとめながらアウトプットを繰り返し、ビジネスパーソンに不可避な
  %span  分かりやすく、端的に話せる能力を身につける為の会議用チャットサイト
  %h6 2.青色のリラックス効果と時間加速を利用し会議時間を効率的に運用する
.login-description
  %h4 ・アカウントの編集はこちら
#account-page.account-page
  .account-page__inner.clearfix
    .account-page__inner--left.account-page__header
      %h2 Edit Account
      %h5 アカウントの編集
      = link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'btn'
      = link_to "トップページに戻る", :back, class: 'btn'
    .account-page__inner--right.user-form
      = form_for(current_user) do |f|
        .field
          .field-label
            = f.label :name
          .field-input
            = f.text_field :name, autofocus: true
        .field
          .field-label
            = f.label :email
          .field-input
            = f.email_field :email
        .actions
          = f.submit "Update", class: 'btn'
